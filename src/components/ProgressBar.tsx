import styled from "styled-components";

const ProgressBar = () => {
  return (
    <Bar>
      {/* populated blocks for number of times */}
    </Bar>
  )

}

const Bar = styled.div`
  width: 1160px;
  height: 24px;
  background: #FFFFFF;
  border-radius: 12px;
  margin: 50px 0px;
`

export default ProgressBar;